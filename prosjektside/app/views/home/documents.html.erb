<h2>Dokumenter</h2>
<!--
<%= link_to "Prosjektskisse", doc_path("Prosjektskisse.pdf") %>
-->
<table>
<% @documents.each do |document| %>

  <tr>
  	<% if document.title.empty? %>
  		<th><%= link_to document.doc.gsub(".pdf", "").split("/")[1].capitalize, doc_path(document.doc) %></td>
  	<% else %>
  		<th><%= link_to document.title, doc_path(document.doc) %></td>
  	<% end %>
  </tr>

<% end %>

</table>